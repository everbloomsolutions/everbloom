name: Build Web Public

# Disabled until the web-public application is implemented.
# Once implemented, replace this file with a workflow similar to build-web-admin.yml.

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Placeholder
        run: echo "web-public is not implemented yet; CI is disabled."
