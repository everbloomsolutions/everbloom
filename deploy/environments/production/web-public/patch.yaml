apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-public
spec:
  template:
    spec:
      containers:
      - name: web-public
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
