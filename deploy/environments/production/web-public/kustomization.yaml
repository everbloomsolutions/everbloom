apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../../apps/web-public/base

replicas:
  - name: web-public
    count: 3

patchesStrategicMerge:
  - patch.yaml
  # Inherits base newTag (updated by CI when UPDATE_GITOPS=true)
