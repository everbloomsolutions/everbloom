apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-admin
spec:
  template:
    spec:
      containers:
      - name: web-admin
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-admin
spec:
  minReplicas: 2
  maxReplicas: 5

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: web-admin
spec:
  minAvailable: 1
