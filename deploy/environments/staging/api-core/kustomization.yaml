apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../../apps/api-core/base

replicas:
  - name: api-core
    count: 2

patchesStrategicMerge:
  - patch.yaml

images:
  - name: api-core
    newTag: staging
