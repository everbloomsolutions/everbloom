apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: development

resources:
  - ../../../apps/web-admin/base

replicas:
  - name: web-admin
    count: 1

patchesStrategicMerge:
  - patch.yaml

images:
  - name: web-admin
    newTag: staging
