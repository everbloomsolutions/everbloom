apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml

commonLabels:
  app: web-public
  component: frontend

images:
  - name: web-public
    newName: ghcr.io/everbloom/web-public
    newTag: latest
