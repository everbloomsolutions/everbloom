apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: web-admin-secrets
  labels:
    app: web-admin
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: SecretStore
  target:
    name: web-admin-secrets
    creationPolicy: Owner
  data:
  - secretKey: api-base-url
    remoteRef:
      key: everbloom/production/web-admin
      property: api-base-url
  - secretKey: socket-url
    remoteRef:
      key: everbloom/production/web-admin
      property: socket-url
